node_modules/
dist/

# Real LinkedIn export archives/PDFs contain personal data (a full export
# has other people's names/companies too). Never commit these.
*.zip
*.pdf
!sample/sample-export.zip

# Where you're expected to drop your own real export locally.
/private/
